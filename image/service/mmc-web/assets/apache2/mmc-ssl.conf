<VirtualHost *:80>
	ServerName ${HOSTNAME}
	Redirect permanent / https://${HOSTNAME}/
</VirtualHost>

<IfModule mod_ssl.c>
	<VirtualHost _default_:443>

		ServerName ${HOSTNAME}
		ServerAdmin ${SERVER_ADMIN}
		ServerPath /mmc

		DocumentRoot /usr/share/mmc

		ErrorLog ${APACHE_LOG_DIR}/error.log
		CustomLog ${APACHE_LOG_DIR}/access.log combined

		Include /etc/apache2/conf-available/vhost-partial-ssl.conf

		SSLCertificateFile    /osixia/service/mmc-web/assets/apache2/ssl/${SSL_CRT_FILENAME}
		SSLCertificateKeyFile /osixia/service/mmc-web/assets/apache2/ssl/${SSL_KEY_FILENAME}
		#SSLCACertificateFile /osixia/service/mmc-web/assets/apache2/ssl/${SSL_CA_CRT_FILENAME}

		<Directory /usr/share/mmc >
			Require all granted
		</Directory>

	</VirtualHost>
</IfModule>
